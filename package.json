{
  "name": "npm-scripts-for-fcc-challanges",
  "version": "1.0.0",
  "description": "npm scripts for fcc challanges",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel es6/src --out-dir es6/dest --source-maps --watch"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "zombie": "^6.1.4"
  }
}
